some commands in case i forget them

(build containers)
sudo docker-compose -f docker-compose-dev.yml build

(up to run container, -d to run in background)
sudo docker-compose -f docker-compose-dev.yml up -d

(update container)
sudo docker-compose -f docker-compose-dev.yml up -d --build

(acces database through psql)
sudo docker-compose -f docker-compose-dev.yml exec referees-db psql -U postgres

(run manage.py command)
sudo docker-compose -f docker-compose-dev.yml run referees python manage.py recreate-db

- sometimes port 80 is already bound to something, use this to find it, usually apache2
sudo netstat -nlptu | grep 80
sudo systemctl stop apache2
